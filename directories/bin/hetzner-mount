#!/usr/bin/env bash
# Maintained in linux-config.org
if [ -d ~/hetzner ]; then
	umount ~/hetzner
	rmdir ~/hetzner
else
	mkdir -p ~/hetzner
	rclone mount --read-only hetzner: ~/hetzner &> /dev/null &
fi
